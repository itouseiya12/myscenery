<h1>新規投稿</h1>
<%= form_with model: @post, url: '/posts', local:true do |f| %>
  <h4>撮影場所</h4>
  <%= f.text_field :location %>
  <h4>感じたこと</h4>
  <%= f.text_area :text %>
  <%= f.submit '投稿' %>
<% end %>
